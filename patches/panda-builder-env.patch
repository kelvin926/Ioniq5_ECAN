diff --git a/SConscript b/SConscript
index 1de1796..10fd6fd 100644
--- a/SConscript
+++ b/SConscript
@@ -5,8 +5,11 @@ import opendbc
 import subprocess
+import sys
 
 PREFIX = "arm-none-eabi-"
-BUILDER = "DEV"
+BUILDER = os.getenv("PANDA_BUILDER", "DEV")
 
 common_flags = []
+if BUILDER == "IONIQ5ECAN":
+  common_flags += ["-DIONIQ5_ECAN_ONLY"]
 
 if os.getenv("RELEASE"):
@@ -102,7 +104,7 @@ def build_project(project_name, project, main, extra_flags):
     BUILDERS={
       'Objcopy': Builder(generator=objcopy, suffix='.bin', src_suffix='.elf')
     },
-    tools=["default", "compilation_db"],
+    tools=(["gcc", "gnulink", "ar", "as", "compilation_db"] if os.name == "nt" else ["default", "compilation_db"]),
   )
 
   startup = env.Object(project["STARTUP_FILE"])
@@ -127,6 +129,6 @@ def build_project(project_name, project, main, extra_flags):
   main_bin = env.Objcopy(f"{project_dir}/main.bin", main_elf)
   sign_py = File(f"./board/crypto/sign.py").srcnode().relpath
-  env.Command(f"./board/obj/{project_name}.bin.signed", main_bin, f"SETLEN=1 {sign_py} $SOURCE $TARGET {cert_fn}")
+  env.Command(f"./board/obj/{project_name}.bin.signed", main_bin, f'"{sys.executable}" "{sign_py}" $SOURCE $TARGET {cert_fn}')
 
 
 
@@ -180,3 +182,4 @@ build_project("body_h7", base_project_h7, "./board/body/main.c", ["-DPANDA_BODY"])
 
 # test files
-SConscript('tests/libpanda/SConscript')
+if os.name != "nt":
+  SConscript('tests/libpanda/SConscript')
